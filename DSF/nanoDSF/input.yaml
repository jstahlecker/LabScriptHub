FILES:
  - FILENAME: "FILENAME1.xlsx"
    CAPILLARY_LIST:
      - 1    # Capillary number
      - 2  
    COLOR_LIST:
      - tab:blue    # Optional: plot color for each capillary
      - red
    LABEL_LIST:
      - TEST1       # Optional: label for each capillary
      - TEST$_2$    # Maybe try quotes if special characters fail

  - FILENAME: "FILENAME2.xlsx"
    CAPILLARY_LIST:
      - 1
    #COLOR_LIST:
    #  - tab:orange
    #LABEL_LIST:


# Output settings
OUTPUT_FOLDER: "."  # Default output folder
OUTPUT_NAME: "combined.png"  # Name of the resulting plot or figure
#TEMP_MIN: START                 # Minimum temperature (°C)
#TEMP_MAX: END                   # Maximum temperature (°C)